package com.example.tigerplayer.ui.main

/**
 * Centralized Compose test tags for MainScreen, following the PrismTestTags convention: tests
 * address nodes by tag rather than by localized display strings.
 */
object MainScreenTestTags {
    /** Root of the standard full-shell bottom NavigationBar (absent while cover-screen is active). */
    const val BOTTOM_NAVIGATION_BAR = "main_screen_bottom_navigation_bar"
}
